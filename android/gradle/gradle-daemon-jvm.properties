# Which JVM runs the Gradle daemon itself. No download URLs on purpose: Gradle
# picks a local JDK 17 and never fetches one. Regenerated by Android Studio on
# sync — keep the version at 17 if it does.
toolchainVersion=17
